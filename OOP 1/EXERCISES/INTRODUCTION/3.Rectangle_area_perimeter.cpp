#include<iostream>
using namespace std;
int main(void)
{
    int area,perimeter,length,width;
    cout << "Enter the length and width respectively -> \n";
    cin >> length >> width;

    area = length * width;
    perimeter = 2*(length + width);

    cout << "The area of the rectangle is " << area << endl << "The perimeter of the circle is " << perimeter << endl;












}
